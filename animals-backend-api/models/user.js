export function User(id, email, password, role, firstName, lastName, authenticationKey) {
    return {
        id,
        email,
        password,
        role,
        firstName,
        lastName,
        authenticationKey
    }
}

